# dependencies (bun install)
node_modules

# output
out
dist
*.tgz

# generated json schemas (regenerated by postinstall)
/typeclaw.schema.json
/cron.schema.json
/auth.schema.json
/secrets.schema.json

# code coverage
coverage
*.lcov

# logs
logs
_.log
report.[0-9]_.[0-9]_.[0-9]_.[0-9]_.json

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# Test artifacts: secrets store writes secrets.json to process.cwd().
# Tests chdir to a tmpdir, but defense in depth — never check these in.
# auth.json is the pre-rename name; still listed for one release as a safety
# net against pulling pre-migration agent folders into this repo.
secrets.json
auth.json

# caches
.eslintcache
.cache
*.tsbuildinfo

# IntelliJ based IDEs
.idea

# Finder (MacOS) folder config
.DS_Store

# Sisyphus working files (plans, scratchpads)
.sisyphus

# Local agent state created by tests / dev runs.
# Anchored with a leading slash so they only match the dev-stage repo root,
# never nested directories that share these names (`src/bundled-plugins/memory/`,
# `src/channels/`, etc. would otherwise be silently ignored).
/sessions/
/memory/
/channels/
/todo/

# Bench: downloaded suites/datasets and per-project deps stay out of git.
# /bench/results/ is intentionally tracked — the scores are the point.
/bench/datasets/
/bench/suites/
/bench/node_modules/
/bench/**/.venv/
/bench/**/__pycache__/


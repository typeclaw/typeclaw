name: Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version (x.y.z)'
        required: true

permissions:
  contents: write
  id-token: write
  packages: write

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Validate version
        # Reject anything that isn't a strict x.y.z before mutating npm/git/
        # GHCR. Pre-flight matters because `npm version ${{ inputs.version }}`
        # interpolates the value into a shell command — without this gate, a
        # malformed `inputs.version` only fails at the Docker tag validation
        # step, AFTER the npm package and git tag are already public.
        env:
          VERSION: ${{ inputs.version }}
        run: |
          if ! [[ "$VERSION" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "::error::Invalid version: '$VERSION' (expected strict x.y.z)"
            exit 1
          fi

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Typecheck
        run: bun run typecheck

      - name: Lint
        run: bun run lint

      - name: Format check
        run: bun run format:check

      - name: Configure git identity
        # Several tests shell out to `git commit` against tmp repos. GitHub
        # Actions runners have no global user.name/user.email, so the commits
        # would silently fail and leave files staged. ci.yml does the same.
        run: |
          git config --global user.email "ci@typeclaw.dev"
          git config --global user.name "TypeClaw CI"

      - name: Test
        # See ci.yml for the rationale on the 60000ms timeout — same network-
        # bound `runInit` tests; keep both workflows in sync.
        run: bun test --timeout 60000

      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          registry-url: 'https://registry.npmjs.org'

      - name: Bump version
        # TypeClaw publishes raw TS sources (no build step), and has no
        # .claude-plugin/plugin.json or skill `version:` frontmatter to sync —
        # package.json is the only place a version lives. `--allow-same-version`
        # makes this idempotent across re-runs.
        run: npm version ${{ inputs.version }} --no-git-tag-version --allow-same-version

      - name: Publish to npm
        # Idempotency guard: if a previous run already published this version,
        # treat the conflict as success so the rest of the workflow can repair
        # whatever halted it (image push, git tag, GitHub Release). npm view
        # returns the version string on success and exits non-zero when the
        # package@version is missing — so a non-empty stdout means "already
        # published, skip."
        run: |
          if [ -n "$(npm view typeclaw@${{ inputs.version }} version 2>/dev/null || true)" ]; then
            echo "typeclaw@${{ inputs.version }} already published, skipping npm publish"
          else
            npm publish --provenance --access public
          fi

      - name: Generate base Dockerfile
        # Same source as base-image.yml (PR validation) — the published base
        # image is a function of src/init/dockerfile.ts at this commit,
        # tagged with the CLI version so downstream consumers pin by
        # `ghcr.io/typeclaw/typeclaw-base:${{ inputs.version }}`.
        run: bun run scripts/emit-base-dockerfile.ts > base.Dockerfile

      - name: Show generated Dockerfile
        run: cat base.Dockerfile

      - uses: docker/setup-qemu-action@v3
        with:
          platforms: arm64

      - uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push base image
        # Tagged `:${{ inputs.version }}` for pinning from the CLI and
        # `:latest` for humans pulling manually. No `:sha-...` tag because
        # the version IS the pin — release.yml owns the version → image
        # mapping as the single writer for version-named tags. Re-pushing
        # the same content to GHCR is idempotent (no immutability policy
        # exists on GHCR as of 2026, so the second push overwrites with
        # identical layers and the tag points at the same digest).
        uses: docker/build-push-action@v6
        with:
          context: .
          file: base.Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/typeclaw-base:${{ inputs.version }}
            ghcr.io/${{ github.repository_owner }}/typeclaw-base:latest
          cache-from: type=registry,ref=ghcr.io/${{ github.repository_owner }}/typeclaw-base:cache
          cache-to: type=registry,ref=ghcr.io/${{ github.repository_owner }}/typeclaw-base:cache,mode=max

      - name: Commit and tag
        # Moved after npm + image publish so a transient failure in either
        # doesn't leave a published git tag pointing at a half-released
        # state. Idempotency guards on both tag creation and push handle
        # re-runs after a tag-step blip: `git tag -f` overwrites a local
        # tag of the same name (harmless on fresh runners where no local
        # tag exists), and `git push --force` on the tag is scoped to the
        # specific tag ref so it cannot rewrite branches or other refs.
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add package.json
          git diff --staged --quiet || git commit -m "${{ inputs.version }}"
          git push origin HEAD
          git tag -f "${{ inputs.version }}"
          git push --force origin "refs/tags/${{ inputs.version }}"

      - name: Create GitHub Release
        # Idempotency guard: skip if the release for this tag already exists
        # (e.g. a previous run got this far but failed elsewhere). `gh release
        # view` exits non-zero when the release doesn't exist.
        run: |
          if gh release view "${{ inputs.version }}" >/dev/null 2>&1; then
            echo "Release ${{ inputs.version }} already exists, skipping"
          else
            gh release create "${{ inputs.version }}" --generate-notes
          fi
        env:
          GH_TOKEN: ${{ github.token }}
